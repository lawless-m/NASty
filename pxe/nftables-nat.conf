#!/usr/sbin/nft -f
# /etc/nftables.d/pxe-nat.conf
# NAT for thin clients on eth1 reaching the home network via eth0

table ip nat {
    chain postrouting {
        type nat hook postrouting priority 100; policy accept;
        oifname "eth0" ip saddr 192.168.99.0/24 masquerade
    }
}
